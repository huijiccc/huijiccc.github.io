# DailyReport 配置文件
topics:
  - 游戏
  - AI科技
  - 股票

# 搜索源
search:
  tavily:
    enabled: true
    api_key: ""  # 需要在 GitHub Secrets 配置

# GitHub Pages 配置
github:
  repo: "daily-report"
  branch: "gh-pages"
  theme: "minimal"

# 定时任务
schedule:
  cron: "0 0 * * *"  # 每天 8 点（UTC 0 点 = 北京时间 8 点）
